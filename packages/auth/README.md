# Memovi Auth

Authentication and authorization domain boundary. This package will own identity-related domain, application, infrastructure, API, and event contracts when Milestone 2 begins.

This package currently defines structure only. It intentionally contains no application code, business logic, persistence models, API endpoints, or placeholder classes.
