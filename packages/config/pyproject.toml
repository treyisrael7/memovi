[project]
name = "memovi-config"
version = "0.1.0"
description = "Configuration package for typed settings and environment loading boundaries. Runtime configuration should be centralized here when application code is introduced."
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
