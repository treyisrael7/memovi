# Memovi Connectors

External source connector domain boundary. This package will own connector contracts, synchronization use cases, infrastructure adapters, API boundaries, and connector events.

This package currently defines structure only. It intentionally contains no application code, business logic, persistence models, API endpoints, or placeholder classes.
