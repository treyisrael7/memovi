[project]
name = "memovi-connectors"
version = "0.1.0"
description = "External source connector domain boundary. This package will own connector contracts, synchronization use cases, infrastructure adapters, API boundaries, and connector events."
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
