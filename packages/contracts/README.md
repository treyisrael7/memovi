# Memovi Contracts

Shared public contracts for communication between Memovi packages. This package should contain stable cross-package schemas, messages, and event contracts when those contracts exist.

This package currently defines structure only. It intentionally contains no application code, business logic, persistence models, API endpoints, or placeholder classes.
