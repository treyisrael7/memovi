[project]
name = "memovi-contracts"
version = "0.1.0"
description = "Shared public contracts for communication between Memovi packages. This package should contain stable cross-package schemas, messages, and event contracts when those contracts exist."
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
