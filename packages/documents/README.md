# Memovi Documents

Document ingestion and document lifecycle domain boundary. This package will own document-specific domain, application, infrastructure, API, and event contracts.

This package currently defines structure only. It intentionally contains no application code, business logic, persistence models, API endpoints, or placeholder classes.
