# Documents API Layer

The API layer exposes document transport schemas and routing. Endpoints will be
added once persistence and composition wiring are implemented.

Current contents:

- `router.py` defines the documents API router scaffold.
- `schemas.py` defines request and response models.
- `dependencies.py` defines dependency placeholders for future use-case wiring.
