[project]
name = "memovi-intelligence"
version = "0.1.0"
description = "Intelligence domain boundary. This package will own AI-facing orchestration, provider boundaries, API contracts, and events while keeping durable knowledge independent from model providers."
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
