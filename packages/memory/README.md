# Memovi Memory

Memory and knowledge organization domain boundary. This package will own durable knowledge concepts, application use cases, infrastructure adapters, API boundaries, and events.

This package currently defines structure only. It intentionally contains no application code, business logic, persistence models, API endpoints, or placeholder classes.
