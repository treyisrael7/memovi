[project]
name = "memovi-memory"
version = "0.1.0"
description = "Memory and knowledge organization domain boundary. This package will own durable knowledge concepts, application use cases, infrastructure adapters, API boundaries, and events."
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
