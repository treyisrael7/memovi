[project]
name = "memovi-search"
version = "0.1.0"
description = "Search and retrieval domain boundary. This package will own search use cases, retrieval contracts, indexing boundaries, API routes, and search-related events."
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
